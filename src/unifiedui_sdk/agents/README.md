# unifiedui_sdk.agents

High-level agent abstractions for building AI agents that integrate with unified-ui.

## Status

**Planned** — This module is not yet implemented.

## Planned Scope

- `ReactAgent` class — a configurable ReACT agent built on LangChain/LangGraph
- Agent engine with built-in tracing and streaming support
- Tool registration and management
- Integration with unified-ui platform service for agent configuration
